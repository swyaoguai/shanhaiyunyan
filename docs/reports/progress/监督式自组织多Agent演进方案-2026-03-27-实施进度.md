# 监督式自组织多 Agent 演进方案实施进度报告

审查时间：2026-03-27  
原始方案已合并清理，后续以 `docs/design/multi-agent/参考Pi的多Agent底座改造方案-2026-05-18.md` 为准。
实施方式：渐进式改造 + 基础设施补齐 + 章节链自治试点 + 项目级任务池深化 + 监督能力增强 + 专项测试验证
更新说明：本文件记录当前代码已实际落地的“第一批开发任务”实施进度、第二阶段“讨论 → 合同确认 → 正式任务池初始化 → Copilot 前端确认入口”闭环改造进度、第三阶段“正式任务池运行态深化”实施进度、第四阶段“主工作区任务池页面化 + 协作轨迹时间线 + 主动轮询与 DOM 测试”实施进度、第五阶段“章节级最小任务市场循环 + 依赖触发 + 评估回流与后端专项测试”实施进度、第六阶段“`summary_orchestrate` 接入章节级任务市场 + 阶段总结依赖编排 + 后端专项测试”实施进度、第七阶段“项目级正式 `task_pool` ready-task 最小调度试点 + 依赖映射 + 合同确认后自动执行 + 后端专项测试”实施进度、第八阶段“项目级正式 `write_chapter` 接入任务池 + 输入装配与前序产物衔接 + 章节结果落盘 + 后端专项测试”实施进度、第九阶段“项目级多章节连续调度 + 停止条件与恢复点显式化 + 合同确认后连续章节试点 + 后端专项测试”实施进度、第十阶段“项目级监督能力增强 + 停止原因写回运行态/协作轨迹 + 状态接口透出 + `fallback_triggered`/显式 `review_required` 停止试点 + 后端专项测试”实施进度、第十一阶段“项目级正式 `summary_orchestrate` 接入任务池 + 阶段总结结果落盘/状态写回 + 后端专项测试”实施进度、第十二阶段“任务池结果引用展示 + 阶段总结文件预览入口 + 前端 DOM 回归测试”实施进度、第十三阶段“世界观 / 大纲 / 章节正文 / 阶段总结多类型产物统一预览 + 前端 DOM 回归测试”实施进度、第十四阶段“任务详情抽屉 + 项目级停止信息展示 + 下一 ready-task 候选信息 + 前端 DOM 回归测试”实施进度、第十五阶段“协作轨迹时间线阶段过滤 + 事件类型过滤 + 前端 DOM 回归测试”实施进度、第十六阶段“协作轨迹时间线过滤后二级能力增强（阶段统计 / 事件类型统计 / 最近匹配高亮 / 过滤条件持久化）+ 前端 DOM 回归测试”实施进度、第十七阶段“协作轨迹时间线过滤后三级能力增强（统计排序 / 最近匹配事件跳转定位 / 常用过滤组合快捷入口 / 过滤条件本地持久化）+ 前端 DOM 回归测试”实施进度、第十八阶段“协作轨迹时间线过滤后四级能力增强（可视化筛选面包屑 / 事件卡片详情展开）+ 前端 DOM 回归测试”实施进度、第十九阶段“协作轨迹时间线过滤后五级能力增强（命名过滤模板保存 / 应用 / 删除）+ 前端 DOM 回归测试”实施进度，以及第二十阶段“协作轨迹命名过滤模板重命名 / 排序首批闭环 + 前端 DOM 回归测试”实施进度。

---

## 一、总体进度概览

依据原方案第 14 节“建议的第一批实际开发任务”，当前实施进度如下：

- ✅ **已完成**：第一阶段 10 项中的 10 项（100%）
- ✅ **已完成**：第二阶段闭环关键项 5 项中的 5 项（100%）
- ✅ **已完成**：第三阶段“运行态深化”关键项 5 项中的 5 项（100%）
- ✅ **已完成**：第四阶段“主工作区协作可视化”关键项 5 项中的 5 项（100%）
- ✅ **已完成**：第五阶段“章节级最小任务市场循环试点”关键项 5 项中的 5 项（100%）
- ✅ **已完成**：第六阶段“阶段总结接入章节级任务市场”关键项 4 项中的 4 项（100%）
- ✅ **已完成**：第七阶段“项目级正式任务池 ready-task 最小调度试点”关键项 4 项中的 4 项（100%）
- ✅ **已完成**：第八阶段“项目级正式 `write_chapter` 接入任务池”关键项 4 项中的 4 项（100%）
- ✅ **已完成**：第九阶段“项目级多章节连续调度试点”关键项 4 项中的 4 项（100%）
- ✅ **已完成**：第十阶段“项目级监督能力增强试点”关键项 5 项中的 5 项（100%）
- ✅ **已完成**：第十一阶段“项目级正式 `summary_orchestrate` 接入任务池”关键项 4 项中的 4 项（100%）
- ✅ **已完成**：第十二阶段“任务池结果引用与预览入口”关键项 3 项中的 3 项（100%）
- ✅ **已完成**：第十三阶段“多类型任务产物统一预览”关键项 3 项中的 3 项（100%）
- ✅ **已完成**：第十四阶段“任务详情抽屉与监督摘要可视化”关键项 4 项中的 4 项（100%）
- ✅ **已完成**：第十五阶段“协作轨迹时间线过滤增强”关键项 3 项中的 3 项（100%）
- ✅ **已完成**：第十六阶段“协作轨迹时间线过滤后二级能力增强”关键项 4 项中的 4 项（100%）
- ✅ **已完成**：第十七阶段“协作轨迹时间线过滤后三级能力增强”关键项 4 项中的 4 项（100%）
- ✅ **已完成**：第十八阶段“协作轨迹时间线过滤后四级能力增强（首批闭环）”关键项 2 项中的 2 项（100%）
- ✅ **已完成**：第十九阶段“协作轨迹时间线过滤后五级能力增强（首批闭环）”关键项 3 项中的 3 项（100%）
- ✅ **已完成**：第二十阶段“协作轨迹命名过滤模板重命名 / 排序首批闭环”关键项 3 项中的 3 项（100%）
- 🔄 **进行中**：1 项（第二十阶段文档同步）
- ❌ **未开始**：0 项

当前已完成的范围聚焦于：

1. 合同层与任务图草案落地
2. Agent 能力声明与能力注册表
3. 任务池与状态流转
4. 消息协议扩展
5. 章节内部链路最小自治试点
6. supervised/fallback 保底策略
7. “讨论 → 合同草案 → 合同确认 → 正式任务池”闭环
8. 章节自治链路对项目级 `task_pool` / `collab_execution_trace` 的持续运行态写回
9. Copilot 前端合同确认入口、任务池摘要与运行态刷新
10. 主工作区任务池页面、协作轨迹时间线与主动轮询
11. 章节级最小任务市场循环、依赖触发与评估回流
12. `summary_orchestrate` 接入章节级任务市场
13. 项目级正式 `task_pool` 的 ready-task 最小调度试点
14. 项目级正式 `write_chapter` 接入正式任务池
15. 后端与前端专项测试补齐
16. 项目级正式任务池的多章节连续调度与停止控制
17. 项目级停止原因写回运行态与协作轨迹，并通过状态接口透出
18. 协作轨迹时间线的阶段过滤、事件类型过滤、统计、最近匹配、快捷过滤与本地持久化
19. 协作轨迹时间线的筛选面包屑、事件详情展开与命名过滤模板保存 / 应用 / 删除
20. 协作轨迹命名过滤模板的重命名与排序首批闭环
18. 项目级正式 `summary_orchestrate` 接入正式任务池并落盘阶段总结产物
19. 任务池结果引用展示与阶段总结文件预览入口
20. 世界观 / 大纲 / 章节正文 / 阶段总结多类型任务产物统一预览
21. 主工作区任务详情抽屉、项目级停止信息展示与下一 ready-task 候选可视化
22. 协作轨迹时间线阶段过滤、事件类型过滤与无匹配提示
23. 协作轨迹时间线阶段统计、事件类型统计、最近匹配事件高亮与过滤条件持久化
24. 协作轨迹时间线可视化筛选面包屑与事件卡片详情展开
25. 协作轨迹命名过滤模板的保存、应用与删除

说明：

- 本轮是“**先协议化，再局部自治化**”的实现轮次
- 没有推翻当前强编排主链
- 保留了当前可运行的创作链路
- 新能力以“可观察、可测试、可回退”为原则接入

---

## 二、与原方案对照的实施状态

## ✅ 任务 1：新增 `CreationContract` 数据结构

**状态：已完成**

**实施位置**：

- `novel_agent/workflow/contracts.py`
- `novel_agent/workflow/__init__.py`

**已落地内容**：

新增合同与任务模型文件，包含：

- `CreationContract`
- `TaskDefinition`
- `TaskDependency`
- `ExecutionPolicy`

并新增默认构建函数：

- `build_default_creation_contract()`
- `build_default_task_graph()`

**实现效果**：

- 用户创作需求不再只停留在 `collected_info`
- 当前创作流程已具备正式合同对象承载能力
- 后续“方案确认 → 自动执行”具备统一契约基础

---

## ✅ 任务 2：新增 `TaskDefinition` / `TaskPool` 数据结构

**状态：已完成**

**实施位置**：

- `novel_agent/workflow/contracts.py`
- `novel_agent/workflow/task_pool.py`
- `novel_agent/workflow/__init__.py`

**已落地内容**：

在任务定义层新增：

- `TaskDefinition`
- `TaskDependency`

在任务池层新增：

- `TaskPool`
- `TaskPoolSnapshot`
- `TaskStatus`

`TaskPool` 已支持：

- 创建任务
- 批量加入任务
- 状态流转
- 任务认领
- 失败重试计数
- review_required 标记
- 依赖图导出
- 快照导出与恢复

**实现效果**：

- 当前章节链已不再只是裸函数调用
- 任务状态现在可以被结构化表达
- 为后续前端任务池展示提供了直接数据基础

---

## ✅ 任务 3：为各 Agent 增加能力声明接口

**状态：已完成**

**实施位置**：

- `novel_agent/agents/base_agent.py`
- `novel_agent/agents/chapter_writer.py`
- `novel_agent/agents/evaluator.py`
- `novel_agent/agents/polisher.py`
- `novel_agent/agents/collab_sub_agents.py`

**已落地内容**：

在 `BaseAgent` 上新增标准能力接口：

- `get_capabilities()`
- `accepts_task()`
- `estimate_cost()`
- `requires_inputs()`
- `produces_outputs()`

并新增：

- `AgentCapability`

同时为章节链相关 Agent 补充了具体能力声明：

- `ContextStrategyAgent`
- `ContentReaderAgent`
- `ContentExpansionAgent`
- `SummaryOrchestratorAgent`
- `ChapterWriterAgent`
- `EvaluatorAgent`
- `PolisherAgent`

**实现效果**：

- “某任务谁能接”已有标准判断方式
- 能力声明已从概念层进入代码层
- 为后续真正的自主认领提供基础

---

## ✅ 任务 4：扩展消息类型以支持任务提案/认领/依赖/重规划

**状态：已完成**

**实施位置**：

- `novel_agent/agents/message_bus.py`
- `novel_agent/agents/__init__.py`

**已落地内容**：

在 `MessageType` 上扩展新增：

- `TASK_PROPOSED`
- `TASK_CLAIMED`
- `TASK_REJECTED`
- `TASK_BLOCKED`
- `DEPENDENCY_REQUIRED`
- `DEPENDENCY_RESOLVED`
- `REVIEW_REQUESTED`
- `REVIEW_ACCEPTED`
- `REPLAN_REQUIRED`
- `PLAN_APPROVED`
- `PLAN_ABORTED`

并补充了消息构造辅助函数：

- `create_task_proposed_message()`
- `create_task_claimed_message()`
- `create_dependency_resolved_message()`

**实现效果**：

- 当前消息总线已具备业务协作协议扩展基础
- 虽然主链尚未完全切成消息驱动，但协议层准备已完成
- 后续从函数编排向协议协作迁移时不需要再重做消息模型

---

## ✅ 任务 5：让 `CommunicatorAgent` 输出合同草案

**状态：已完成（第二阶段闭环版）**

**实施位置**：

- `novel_agent/agents/router_agent.py`
- `novel_agent/workflow/coordinator.py`

**已完成内容**：

当前合同草案虽然仍由 Router/Coordinator 协同装配，但已经接入真实对话链并可在用户侧确认：

1. 聊天入口在识别 `create_novel` 时默认改为“先讨论、后确认”，不再直接自动执行
2. `RouterAgent` 在 `auto_execute = false` 场景下会返回：
   - `action = confirm_creation_contract`
   - `requires_confirmation = true`
   - `params.creation_contract`
   - `params.task_graph_draft`
   - `params.contract_status = draft`
3. 合同草案与任务图草案会即时落盘到项目状态

已落盘的项目状态键包括：

- `creation_contract`
- `task_graph_draft`

**实现效果**：

- 对话链现在已经能够向前端返回“待确认合同草案”
- 用户不再必须通过传统创作面板手动补齐流程
- 原方案中“讨论阶段先形成方案草案”的目标已进入真实交互链路

---

## ✅ 任务 6：在 `RouterAgent` 中加入“方案确认后自动建任务池”的逻辑

**状态：已完成（第二阶段闭环版）**

**实施位置**：

- `novel_agent/agents/router_agent.py`
- `novel_agent/workflow/coordinator.py`

**已落地内容**：

Router 侧已增加：

- 合同草案构建
- 合同持久化
- 任务图草案构建
- `confirm_creation_contract` 响应分支

Coordinator 侧已增加正式任务池初始化方法：

- `initialize_task_pool_from_contract()`

确认合同后，系统会自动生成并落盘：

- `creation_contract`
- `task_graph_draft`
- `task_pool`
- `collab_execution_trace`

前端侧已增加确认入口：

- Copilot 中渲染“创作合同草案”卡片
- 用户点击确认后调用 `/api/novel/contract/confirm`
- 确认成功后自动渲染任务池摘要卡片

**实现效果**：

- 已不再只是“生成任务图草案”
- 已具备“用户确认 → 初始化正式任务池”的完整闭环
- 同时后续章节自治任务已经开始在该正式任务池上持续写回运行态
- 这是原方案第 7 节和第 10 节阶段四的首个真正落地版本

---

## ✅ 任务 7：在章节内部链路先试点自治认领

**状态：已完成**

**实施位置**：

- `novel_agent/workflow/coordinator.py`

**已落地内容**：

在章节链中新增最小自治执行入口：

- `_run_autonomous_task()`

并将章节内部多个步骤改为经由该入口执行：

- `context_plan`
- `content_read`
- `write_chapter`
- `evaluate_chapter`
- `polish_chapter`
- `expand_content`
- `summary_orchestrate`

章节结果中已新增：

- `autonomy_trace`

用于记录每个子任务的：

- 候选 Agent
- 实际执行 Agent
- 任务池快照

**实现效果**：

- 章节链至少一段流程已不再完全依赖 Coordinator 直接点名
- 当前已实现“候选查询 → 选择执行 → 结果记录”的最小自治试点
- 满足原方案阶段三的最低完成标准

---

## ✅ 任务 8：保留强编排回退模式

**状态：已完成**

**实施位置**：

- `novel_agent/workflow/contracts.py`
- `novel_agent/workflow/coordinator.py`

**已落地内容**：

在执行策略层新增：

- `supervised_mode`
- `fallback_to_orchestrated`

在 Coordinator 中新增保底配置：

- `self.supervised_mode = True`
- `self.fallback_to_orchestrated = True`

并在 `_run_autonomous_task()` 中实现：

1. supervised 模式下优先尝试能力匹配 Agent
2. 当自治执行失败时自动记录错误
3. 若允许 fallback，则自动切回强编排 Agent 执行
4. 输出执行轨迹字段：
   - `execution_mode`
   - `fallback_used`
   - `fallback_agent`
   - `autonomous_error`

**实现效果**：

- 自治链路异常时可自动退回当前稳定路径
- 满足原方案第 12 节风险控制要求
- 当前属于“监督式自组织”，而非无保底自治

---

## ✅ 任务 9：为任务池状态与协作轨迹补测试

**状态：已完成（已扩展到第二阶段闭环）**

**实施位置**：

- `novel_agent/tests/test_supervised_collab_foundation.py`

**已补测试范围**：

1. 合同模型 roundtrip
2. `TaskDefinition` / `TaskDependency` 序列化恢复
3. 默认合同与任务图构建
4. `BaseAgent` 默认能力声明行为
5. `AgentCapabilityRegistry` 候选查询与优先级排序
6. `TaskPool` 状态流转、依赖满足、评审标记、恢复
7. `NovelCoordinator._run_autonomous_task()`：
   - 正常候选执行
   - 自治失败后 fallback
   - `supervised_mode=False` 时直接走回退 Agent
   - 项目级 `task_pool` 运行态写回
   - 项目级 `collab_execution_trace` 事件追加
8. `NovelCoordinator.initialize_task_pool_from_contract()`：
   - 合同确认后写入正式任务池
   - 写入 `collab_execution_trace`
   - 任务池元数据与来源字段正确
9. `RouterAgent` 在 `create_novel + auto_execute=false` 场景下：
   - 返回待确认合同草案
   - 返回 `creation_contract`
   - 返回 `task_graph_draft`
   - 返回 `contract_status = draft`
10. Copilot 运行态恢复：
   - 刷新 workflow 状态
   - 同步刷新 `task_pool` / `creation_contract`

**测试结果**：

```bash
pytest ./novel_agent/tests/test_supervised_collab_foundation.py -q
17 passed in 53.25s

npx vitest run ./frontend-tests/copilot-workflow.dom.test.js ./frontend-tests/copilot-commands.dom.test.js
2 passed / 9 tests passed
```

**额外收益**：

本轮测试过程中还暴露并修复了一个真实问题：

- `coordinator.py` 通过包级 `agents` 导入造成循环依赖

已改为直接模块导入，消除：

- `agents -> router -> workflow -> coordinator -> agents`

这条循环导入链。

---

## ✅ 任务 10：为前端增加合同视图、任务池视图、Agent 协作状态视图

**状态：已完成第一轮 Copilot 侧 UI 实装**

**已落地内容**：

### 1. 合同视图
已在 Copilot 前端增加：

- 合同卡片渲染
- 合同范围/约束/任务预览展示
- “确认合同并初始化任务池”按钮

### 2. 任务池视图
已在 Copilot 前端增加：

- 任务池摘要卡片
- 状态统计
- 前 6 个任务预览
- 候选 Agent 展示
- 从 `/api/novel/status` 主动刷新运行态任务池快照

### 3. 协作状态视图
原有 Copilot workflow 面板继续保留，用于承接：

- 当前执行状态
- 当前 Agent / 当前阶段
- 产物文件列表
- 工作流回显

### 4. 当前实现边界
在上一轮时，前端仍是“Copilot 侧最小闭环 UI”；本轮已继续扩展到主工作区独立协作视图。

**实现效果**：

- 用户现在已经可以在 Copilot 中看到待确认合同
- 点击确认后能够真正初始化任务池并看到任务池摘要
- 主工作区已新增独立的任务池页面入口
- 主工作区已可展示完整任务列表、状态、候选 Agent、认领 Agent、依赖与重试次数
- 主工作区已可展示协作轨迹时间线
- 任务池页面已支持手动刷新与前端主动轮询刷新
- 任务池与协作状态已经具备从 Copilot 摘要卡片到主工作区完整页面的第二轮可视化能力

---

## 三、本轮额外完成的关键实现

除原方案“第一批任务”外，本轮还完成了若干对后续演进很关键的基础工作。

### 1. 合同与任务图已接入真实创作入口

在创作启动时，系统已会：

1. 创建项目
2. 生成合同草案
3. 生成任务图草案
4. 落盘保存到项目状态
5. 在初始化事件中向外返回 `creation_contract`

这意味着后续前端要展示合同，不必重新改主链才能拿到数据。

### 2. 章节协作轨迹已从“结果级”扩展到“项目运行态级”

早期章节结果中已增加：

- `autonomy_trace`

本轮进一步补上了项目级持续运行态：

- `task_pool`
- `collab_execution_trace`

并在 `_run_autonomous_task()` 执行过程中持续写回：

- task_registered
- task_started
- task_failed
- task_fallback_started
- task_completed

这意味着系统不再只是“章节结束后看到一次性轨迹”，而是已经具备了项目运行过程中可持续观察的协作状态基础。

### 3. 循环依赖风险被顺手收敛

测试过程中暴露的循环导入问题，说明原有包级导出在架构演进时风险会放大。  
当前已通过让 Coordinator 直接从具体模块导入 Agent 类，避免继续扩大耦合。

这项修复对后续“更细粒度自治化”是必要的稳定性前提。

---

## 四点五、第四阶段新增完成项：主工作区任务池页面化与协作轨迹时间线

### ✅ 任务池完整页面化
**状态：已完成**

**实施位置**：

- `novel_agent/web/static/continuous_write.js`

**已落地内容**：

在协作创作导航 [`renderMultiAgentWriteNavPanel()`](../novel_agent/web/static/continuous_write.js:576) 中新增固定入口：

- `任务池与协作轨迹`

并新增独立工作区渲染逻辑：

- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:428)
- [`openCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:555)

页面已展示：

- 任务池总体状态徽标
- 完整任务列表
- `task_type`
- 当前状态
- 候选 Agent
- 已认领 Agent
- 重试次数
- 依赖任务
- 合同来源信息

**实现效果**：

- 任务池不再只以 Copilot 摘要卡片存在
- 用户可以在主工作区查看完整任务池运行态
- “任务池完整页面化”已从建议项变为已落地能力

### ✅ 协作轨迹时间线视图
**状态：已完成**

**实施位置**：

- `novel_agent/web/static/continuous_write.js`

**已落地内容**：

新增时间线渲染函数：

- [`renderCollabExecutionTimeline()`](../novel_agent/web/static/continuous_write.js:387)

时间线已对项目级 `collab_execution_trace.events` 做可视化展示，当前可承接的事件包括：

- `task_registered`
- `task_started`
- `task_failed`
- `task_fallback_started`
- `task_completed`

每条事件已展示：

- 事件类型
- 任务标题 / 任务类型
- Agent
- 状态
- 时间戳
- 任务 ID（若存在）

**实现效果**：

- 协作轨迹不再只是后端落盘字段
- 用户已可以在前端主工作区观察自治执行过程
- 为后续“可审计、可调试、可观察”的协作流继续打基础

### ✅ 前端主动轮询 / 按需刷新
**状态：已完成**

**实施位置**：

- `novel_agent/web/static/app-core.js`
- `novel_agent/web/static/continuous_write.js`

**已落地内容**：

在运行态刷新基础上继续增加：

- [`startNovelCollabRuntimePolling()`](../novel_agent/web/static/app-core.js:601)
- [`stopNovelCollabRuntimePolling()`](../novel_agent/web/static/app-core.js:594)
- [`shouldPollNovelCollabRuntime()`](../novel_agent/web/static/app-core.js:585)

并接入：

1. 页面可见性变化时自动停启轮询
2. 模块切换时自动停启轮询
3. 打开任务池页面时自动启动轮询
4. 任务池页保留手动刷新按钮作为按需刷新入口

当前策略为：

- 仅在 `write` 模块且当前子视图为 `task-pool` 时轮询
- 页面隐藏时自动停止
- 默认轮询间隔：15 秒

**实现效果**：

- 任务池页面已具备持续更新能力
- 不会在非协作任务页无意义轮询
- 前端轮询与手动刷新形成互补

### ✅ DOM 测试补齐
**状态：已完成**

**实施位置**：

- `frontend-tests/copilot-workflow.dom.test.js`
- `frontend-tests/copilot-commands.dom.test.js`

**新增测试范围**：

1. 打开任务池工作区后：
   - 会请求 `/api/novel/status`
   - 会渲染“协作任务池”
   - 会渲染任务列表
   - 会渲染协作轨迹时间线
2. 任务池视图激活时：
   - 会启动轮询
   - 会重新请求运行态接口
   - 会触发任务池页面重渲染
3. 合同确认、Copilot 卡片、slash command 等原有行为保持回归通过

**测试结果**：

```bash
npx vitest run ./frontend-tests/copilot-workflow.dom.test.js ./frontend-tests/copilot-commands.dom.test.js
2 passed / 9 tests passed
```

**补充说明**：

测试编写过程中还修复了两类前端细节问题：

- 任务池页测试中的轮询状态污染
- `multiAgentWriteState` 重复挂载到 `window` 的冗余代码

---

## 四点六、第五阶段新增完成项：章节级最小任务市场循环与评估回流

### ✅ 章节级最小任务市场循环
**状态：已完成**

**实施位置**：

- [`NovelCoordinator._build_chapter_autonomous_task_pool()`](../novel_agent/workflow/coordinator.py:1365)
- [`NovelCoordinator._execute_chapter_task_market()`](../novel_agent/workflow/coordinator.py:1476)
- [`NovelCoordinator._write_single_chapter_internal()`](../novel_agent/workflow/coordinator.py:2269)

**已落地内容**：

在章节内部不再只通过多个独立的 [`_run_autonomous_task()`](../novel_agent/workflow/coordinator.py:2033) 顺序包裹执行，而是新增章节级任务池构建与循环执行逻辑，形成最小任务市场试点。

当前章节级任务池已纳入的任务包括：

- `context_plan`
- `content_read`
- `write_chapter`
- `evaluate_chapter`
- `polish_chapter`
- `expand_content`

并支持：

- 基于 `depends_on` 的 ready task 选择
- 任务依赖满足后的自动推进
- 各任务执行结果统一汇入章节级 `results`
- 最终写回章节 `autonomy_trace`

**实现效果**：

- 章节内部链已从“多个自治包装函数顺序调用”推进到“同一章节任务池中的依赖触发式循环”
- 第五阶段第一次把“任务市场”从概念层推进到真实执行层
- 为后续把更多章节任务接入统一任务市场打下直接基础

### ✅ `ContextStrategy / ContentReader` 依赖触发式串接
**状态：已完成**

**实施位置**：

- [`ContextStrategyAgent`](../novel_agent/agents/collab_sub_agents.py:102)
- [`ContentReaderAgent`](../novel_agent/agents/collab_sub_agents.py:150)
- [`NovelCoordinator._execute_chapter_task_market()`](../novel_agent/workflow/coordinator.py:1476)

**已落地内容**：

章节任务池内已明确建立：

- `content_read.depends_on = [context_plan]`

执行时由任务市场循环自动完成：

1. 先执行 `context_plan`
2. 将其 `strategy` 结果注入 `content_read.inputs`
3. 依赖满足后再触发 `content_read`

**实现效果**：

- 这一段链路已经不再由 Coordinator 手工串接
- “依赖满足后自动推进”首次进入真实章节执行逻辑
- 满足原方案中“先从章节内部子链试点依赖触发”的目标

### ✅ `Evaluator / Polisher` 的 `review_required` 与失败回流
**状态：已完成**

**实施位置**：

- [`NovelCoordinator._build_chapter_autonomous_task_pool()`](../novel_agent/workflow/coordinator.py:1365)
- [`NovelCoordinator._execute_chapter_task_market()`](../novel_agent/workflow/coordinator.py:1476)

**已落地内容**：

章节任务池中已加入：

- `evaluate_chapter`
- `polish_chapter`
- `expand_content`

并形成如下最小回流逻辑：

1. `evaluate_chapter` 在章节正文后执行，保留 `review_required`
2. `polish_chapter` 默认以 `blocked` 进入任务池
3. 若评估结果 `passed = false`：
   - 自动解读评估建议
   - 触发 `polish_chapter`
   - 将修订结果作为后续内容来源
4. 若评估结果 `passed = true`：
   - `polish_chapter` 标记为 `aborted`
   - 直接跳过修订
5. `expand_content` 优先衔接修订后的内容，否则衔接原始正文

**实现效果**：

- 已具备“评估失败 → 修订回流 → 再进入后续处理”的章节级自治闭环
- 已开始把 `review_required` 从展示性状态推进到真实控制信号
- 这比前一阶段“只是顺序调用评估/润色”更接近监督式自组织协作

### ✅ 后端专项测试补齐
**状态：已完成**

**实施位置**：

- [`novel_agent/tests/test_supervised_collab_foundation.py`](../novel_agent/tests/test_supervised_collab_foundation.py)

**新增测试范围**：

新增覆盖了章节级任务市场循环两条关键路径：

1. **评估失败 → 润色回流 → 扩写收尾**
   - 断言 `context_plan / content_read / write_chapter / evaluate_chapter / polish_chapter / expand_content` 均进入 `completed`
   - 断言评估失败后确实触发润色
   - 断言扩写使用修订后的结果继续推进

2. **评估通过 → 跳过润色 → 直接扩写**
   - 断言 `polish_chapter` 被标记为 `aborted`
   - 断言不会产生 `polish_chapter` 执行结果
   - 断言 `expand_content` 仍可继续完成

同时保留原有基础测试全部通过。

**测试结果**：

```bash
pytest ./novel_agent/tests/test_supervised_collab_foundation.py -q
17 passed in 53.25s
```

## 四点七、第六阶段新增完成项：`summary_orchestrate` 接入章节级任务市场

### ✅ 阶段总结任务接入章节级任务市场
**状态：已完成**

**实施位置**：

- [`NovelCoordinator._build_chapter_autonomous_task_pool()`](../novel_agent/workflow/coordinator.py:1365)
- [`NovelCoordinator._execute_chapter_task_market()`](../novel_agent/workflow/coordinator.py:1494)
- [`NovelCoordinator._write_single_chapter_internal()`](../novel_agent/workflow/coordinator.py:2306)

**已落地内容**：

在章节级任务池中新增动态阶段总结任务：

- 当章节号满足 `10 / 20 / 30 ...` 时
- 自动创建 `summary_orchestrate`
- 其依赖为当前章的 [`expand_content`](../novel_agent/workflow/coordinator.py:1457) 完成

同时在执行循环中补上任务输入注入：

- `start_chapter`
- `end_chapter`
- `chapters`

其中 `chapters` 由：

- `previous_chapters[-9:]`
- 当前章最终正文产物

共同组成。

**实现效果**：

- 阶段总结不再是章节收尾时的独立硬编码分支
- 已正式纳入同一章节级任务市场循环
- 阶段性产物开始复用统一依赖调度机制

### ✅ `summary_orchestrate` 复用章节级任务市场结果与阶段产物
**状态：已完成**

**实施位置**：

- [`NovelCoordinator._execute_chapter_task_market()`](../novel_agent/workflow/coordinator.py:1494)
- [`NovelCoordinator._write_single_chapter_internal()`](../novel_agent/workflow/coordinator.py:2306)

**已落地内容**：

阶段总结任务现在会优先复用章节级任务市场中的：

- `expand_content` 最终正文
- `write_chapter` 正文兜底结果
- `previous_chapters` 中的历史章节内容

并且章节结果现在直接消费任务市场中的：

- `summary`
- `summary_payload`

不再额外调用一次独立 [`_run_autonomous_task()`](../novel_agent/workflow/coordinator.py:2070) 执行阶段总结。

**实现效果**：

- 消除了旧的重复执行分支
- `summary_orchestrate` 已与章节级任务市场产物打通
- `stage_summaries` 落盘继续保留，但其来源已经改成任务市场统一结果

### ✅ 第六阶段后端专项测试补齐
**状态：已完成**

**实施位置**：

- [`novel_agent/tests/test_supervised_collab_foundation.py`](../novel_agent/tests/test_supervised_collab_foundation.py)

**新增测试范围**：

新增了“第 10 章触发阶段总结任务”的专项断言：

- 断言 `summary_orchestrate` 会在章节级任务市场中进入 `completed`
- 断言返回正确的 `summary`
- 断言 `summary_payload.end_chapter == 10`

**测试结果**：

```bash
pytest ./novel_agent/tests/test_supervised_collab_foundation.py -q
17 passed in 53.25s
```

## 四点八、第七至第九阶段新增完成项：项目级正式任务池调度深化与多章节连续执行

### ✅ 项目级正式 `task_pool` ready-task 最小调度试点
**状态：已完成**

**实施位置**：

- [`NovelCoordinator.initialize_task_pool_from_contract()`](../novel_agent/workflow/coordinator.py:1204)
- [`NovelCoordinator._hydrate_project_task_depends_on()`](../novel_agent/workflow/coordinator.py:1281)
- [`NovelCoordinator.execute_project_ready_tasks()`](../novel_agent/workflow/coordinator.py:1403)
- [`confirm_creation_contract()`](../novel_agent/web/routes/novel.py:193)

**已落地内容**：

在项目级正式任务池中，已实现第一轮 ready-task 调度闭环：

1. 用户确认合同后，由 [`initialize_task_pool_from_contract()`](../novel_agent/workflow/coordinator.py:1204) 初始化正式 `task_pool`
2. 通过 [`_hydrate_project_task_depends_on()`](../novel_agent/workflow/coordinator.py:1281) 将合同中的语义依赖映射为真实 `depends_on`
3. 再由 [`execute_project_ready_tasks()`](../novel_agent/workflow/coordinator.py:1403) 基于 [`get_ready_tasks()`](../novel_agent/workflow/task_pool.py:259) 拉起首批 ready-task
4. 首轮试点已支持：
   - `build_world`
   - `build_outline`

**实现效果**：

- 项目级正式 `task_pool` 不再只是初始化后静态展示
- 合同确认后已经会自动推进第一批正式任务
- 项目级调度首次从“有状态”进入“可执行”

### ✅ 项目级正式 `write_chapter` 接入任务池
**状态：已完成**

**实施位置**：

- [`NovelCoordinator._load_project_outline_rows()`](../novel_agent/workflow/coordinator.py:1313)
- [`NovelCoordinator._load_project_previous_chapters()`](../novel_agent/workflow/coordinator.py:1320)
- [`NovelCoordinator._persist_project_ready_chapter_result()`](../novel_agent/workflow/coordinator.py:1340)
- [`NovelCoordinator.execute_project_ready_tasks()`](../novel_agent/workflow/coordinator.py:1403)

**已落地内容**：

项目级 `write_chapter` 已正式纳入 ready-task 调度支持集，并补上了最小可运行所需的输入装配与结果落盘能力：

1. 从项目 `outline` 行装配：
   - `chapter_number`
   - `chapter_title`
   - `chapter_outline`
2. 从前序章节内容构造：
   - `previous_chapters`
3. 章节执行后统一写回：
   - 项目级 `outline`
   - `chapters/*.md`
   - 正式任务池 `result_ref`

同时项目级章节任务继续复用章节内部任务市场闭环，而不是退回单纯硬编码分支。

**实现效果**：

- 正式任务池中的 `write_chapter` 已不再是占位任务
- 项目级调度已能真正推进到正文创作
- 项目级任务池与章节级任务市场实现了首轮贯通

### ✅ 项目级多章节连续调度与停止条件显式化
**状态：已完成**

**实施位置**：

- [`NovelCoordinator.execute_project_ready_tasks()`](../novel_agent/workflow/coordinator.py:1403)
- [`confirm_creation_contract()`](../novel_agent/web/routes/novel.py:193)

**已落地内容**：

在本轮中，项目级调度继续从“可执行单章”推进到“可连续执行多章，并显式控制停止条件”：

1. [`execute_project_ready_tasks()`](../novel_agent/workflow/coordinator.py:1403) 新增：
   - `max_chapter_tasks`
   - `stop_reason`
   - `chapter_tasks_executed`
2. 当前已支持的停止原因包括：
   - `unsupported_task_type`
   - `max_tasks_reached`
   - `max_chapter_tasks_reached`
3. 合同确认后的自动执行已扩大为“小批量连续章节试点”：
   - `max_tasks = 4`
   - `max_chapter_tasks = 2`

这意味着系统现在不仅能继续推进后续 `write_chapter`，还能明确告诉前端与调用方“为什么此轮停下”。

**实现效果**：

- 多章节连续调度不再依赖隐式循环边界
- 恢复点继续锚定在项目级 `task_pool`、`outline` 与已落盘章节文件
- 项目级正式任务池开始具备“可连续推进 + 可解释停止”的最小监督式调度特征

### ✅ 第九阶段后端专项测试补齐
**状态：已完成**

**实施位置**：

- [`novel_agent/tests/test_supervised_collab_foundation.py`](../novel_agent/tests/test_supervised_collab_foundation.py)

**新增测试范围**：

本轮新增/调整了项目级调度专项测试，重点覆盖：

1. 项目级正式任务池可执行：
   - `build_world`
   - `build_outline`
   - 首个 `write_chapter`
2. 新增多章节连续调度专项断言：
   - 连续执行两章 `write_chapter`
   - 第三章保持 `pending`
   - 返回 `chapter_tasks_executed == 2`
   - 返回 `stop_reason = max_chapter_tasks_reached`
3. 同步更新旧测试断言：
   - 当前返回增加 `stop_reason`
   - 当前返回增加 `chapter_tasks_executed`

**测试结果**：

```bash
pytest ./novel_agent/tests/test_supervised_collab_foundation.py -q
20 passed in 79.44s
```

## 四点九、第十阶段新增完成项：项目级监督能力增强与停止信息透出

### ✅ 项目级停止原因写回 `task_pool.metadata` 与协作轨迹
**状态：已完成**

**实施位置**：

- [`NovelCoordinator.execute_project_ready_tasks()`](../novel_agent/workflow/coordinator.py:1403)
- [`NovelCoordinator._append_collab_execution_event()`](../novel_agent/workflow/coordinator.py:1623)

**已落地内容**：

在项目级 ready-task 调度结束后，当前会把调度批次信息统一写回运行态：

1. 写入 [`task_pool.metadata`](../novel_agent/workflow/task_pool.py:68)：
   - `project_ready_execution.updated_at`
   - `project_ready_execution.executed_task_count`
   - `project_ready_execution.chapter_tasks_executed`
   - `project_ready_execution.stopped_on_task_type`
   - `project_ready_execution.stop_reason`
   - `project_ready_execution.max_tasks`
   - `project_ready_execution.max_chapter_tasks`
2. 追加项目级协作轨迹事件：
   - `project_ready_execution_cycle`

**实现效果**：

- 项目级停止信息不再只是接口即时返回值
- 现在已经成为项目运行态的一部分
- 为恢复、审计、前端展示提供了稳定锚点

### ✅ 项目级停止条件扩展：`fallback_triggered` 与显式 `review_required`
**状态：已完成**

**实施位置**：

- [`NovelCoordinator.execute_project_ready_tasks()`](../novel_agent/workflow/coordinator.py:1403)
- [`NovelCoordinator._run_autonomous_task()`](../novel_agent/workflow/coordinator.py:2401)

**已落地内容**：

本轮在保留原有停止条件基础上，进一步扩展了项目级监督停止逻辑：

1. 当自治任务实际发生 fallback 时：
   - 当前批次停止
   - `stop_reason = fallback_triggered`
2. 对 `review_required` 采用显式 opt-in：
   - 仅当任务本身 `review_required = true`
   - 且 `metadata.stop_on_review_required = true`
   - 才会触发项目级停止
3. 同时修正了 [`fallback_used`](../novel_agent/workflow/coordinator.py:2490) 的语义：
   - 普通监督模式正常命中主候选 Agent 时不误判为 fallback
   - `supervised_mode = false` 且直接走回退 Agent 时仍正确标记 `fallback_used = true`

**实现效果**：

- 避免默认大纲任务因 `review_required` 提前误停整条项目级链路
- 项目级停止条件从“任务上限型”开始走向“监督策略型”
- 为后续继续加入预算/冲突/超时停机条件提供了模式模板

### ✅ 状态接口透出项目级调度停止信息
**状态：已完成**

**实施位置**：

- [`get_status()`](../novel_agent/web/routes/novel.py:240)

**已落地内容**：

状态接口现在除继续返回：

- `task_pool`
- `collab_execution_trace`
- `creation_contract`

外，还新增直接返回：

- `project_ready_execution`

其数据来源为：

- `task_pool.metadata.project_ready_execution`

**实现效果**：

- 前端不必自行深挖 `task_pool.metadata`
- 项目级停止原因、停止点、连续执行章节数已可直接消费
- 为后续前端补停止原因可视化打下接口基础

### ✅ 第十阶段后端专项测试补齐
**状态：已完成**

**实施位置**：

- [`novel_agent/tests/test_supervised_collab_foundation.py`](../novel_agent/tests/test_supervised_collab_foundation.py)

**新增测试范围**：

本轮新增/调整了以下专项测试：

1. `fallback_triggered` 停止：
   - 断言项目级任务在 fallback 后立即停止
   - 断言停止原因写入 `task_pool.metadata.project_ready_execution`
   - 断言协作轨迹末尾追加 `project_ready_execution_cycle`
2. 显式 `review_required` 停止：
   - 断言仅在 `metadata.stop_on_review_required = true` 时触发项目级停止
3. 状态接口透出：
   - 断言 [`get_status()`](../novel_agent/web/routes/novel.py:240) 返回 `project_ready_execution`
4. 原有项目级 world/outline/章节/多章节测试继续全部回归通过

**测试结果**：

```bash
pytest ./novel_agent/tests/test_supervised_collab_foundation.py -q
23 passed in 93.75s
```

## 四点十、第十一阶段新增完成项：项目级正式 `summary_orchestrate` 接入任务池

### ✅ 项目级正式 `summary_orchestrate` 纳入正式 `task_pool`
**状态：已完成**

**实施位置**：

- [`build_default_task_graph()`](../novel_agent/workflow/contracts.py:301)
- [`NovelCoordinator._hydrate_project_task_depends_on()`](../novel_agent/workflow/coordinator.py:1281)

**已落地内容**：

本轮将阶段总结任务从“章节级任务市场专属能力”继续扩展到项目级正式任务池：

1. 在默认合同任务图中新增项目级 `summary_orchestrate`
2. 仅在 `10 / 20 / 30 ...` 章节节点自动创建阶段总结任务
3. 默认产物声明改为：
   - `stage_summaries/第X-Y章-剧情总结.md`
4. 在项目级依赖映射中追加：
   - `summary_orchestrate.depends_on = 对应 end_chapter 的 write_chapter`

**实现效果**：

- 项目级正式任务池已不再只覆盖 world / outline / chapter
- 阶段总结首次成为正式任务池中的一等任务
- 项目级调度开始具备“章节产物 → 阶段总结产物”的正式依赖链

### ✅ 项目级阶段总结输入装配与结果落盘
**状态：已完成**

**实施位置**：

- [`NovelCoordinator._load_project_stage_summary_chapters()`](../novel_agent/workflow/coordinator.py:1422)
- [`NovelCoordinator._persist_project_stage_summary_result()`](../novel_agent/workflow/coordinator.py:1455)
- [`NovelCoordinator.execute_project_ready_tasks()`](../novel_agent/workflow/coordinator.py:1494)

**已落地内容**：

项目级 ready-task 调度现已支持真正执行 `summary_orchestrate`：

1. 调度循环已把 `summary_orchestrate` 纳入支持集
2. 执行前自动从项目 `outline` 装配：
   - `start_chapter`
   - `end_chapter`
   - `chapters`
3. 执行后统一写回：
   - `collab_stage_summaries`
   - `stage_summaries/第X-Y章-剧情总结.md`
   - 正式任务池 `result_ref`
4. 同时补充运行态元数据：
   - `summary_path`

**实现效果**：

- 项目级阶段总结已不再停留在合同草案的占位任务
- 正式任务池中的阶段总结结果已具备稳定文件锚点
- 项目状态、任务池结果引用与磁盘产物三者已贯通

### ✅ 第十一阶段后端专项测试补齐
**状态：已完成**

**实施位置**：

- [`novel_agent/tests/test_supervised_collab_foundation.py`](../novel_agent/tests/test_supervised_collab_foundation.py)

**新增测试范围**：

本轮新增并通过了两类专项断言：

1. 默认任务图会在 10 章节点自动生成项目级 `summary_orchestrate`
2. 当项目级前序 `write_chapter` 已完成时：
   - `summary_orchestrate` 可被 ready-task 调度执行
   - 阶段总结文件会真正落盘
   - `collab_stage_summaries` 会同步写回
   - 正式任务池 `result_ref` 指向实际产物文件

**测试结果**：

```bash
pytest ./novel_agent/tests/test_supervised_collab_foundation.py -q
25 passed in 99.92s
```

## 四点十一、第十二阶段新增完成项：任务池结果引用展示与阶段总结文件预览入口

### ✅ 任务池结果引用展示
**状态：已完成**

**实施位置**：

- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:426)

**已落地内容**：

在主工作区任务池页面中，已为任务卡片补充结果引用展示区，当前会优先读取：

- `task.result_ref`
- `task.metadata.summary_path`

并补充展示：

- 结果类型 `result_kind`
- 阶段总结区间 `summary_range`
- 实际产物路径

**实现效果**：

- 任务池页面不再只能看到“任务是否完成”
- 用户已能直接看到任务完成后对应的结果锚点
- 阶段总结产物首次从“后端可用字段”提升为“前端可见信息”

### ✅ 阶段总结文件预览入口
**状态：已完成**

**实施位置**：

- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:426)
- [`previewWorkflowFile()`](../novel_agent/web/static/app-core.js:703)

**已落地内容**：

在存在结果文件路径时，任务卡片会渲染：

- `预览结果` 按钮

点击后会直接复用已有文件预览能力，对：

- `stage_summaries/*.md`
- 其他具备 `result_ref` 的任务产物

执行统一预览。

同时已将 [`previewWorkflowFile()`](../novel_agent/web/static/app-core.js:703) 挂载到 `window`，供任务池页面直接调用。

**实现效果**：

- 前端已可直接从任务池页面打开阶段总结文件
- 结果引用从“只显示路径”提升为“可点击、可预览”
- 为后续继续扩展章节正文、世界观、大纲等任务产物预览提供了统一入口

### ✅ 第十二阶段前端 DOM 测试补齐
**状态：已完成**

**实施位置**：

- [`frontend-tests/copilot-workflow.dom.test.js`](../frontend-tests/copilot-workflow.dom.test.js)

**新增测试范围**：

本轮新增并通过了任务池结果引用相关断言：

1. 打开任务池工作区后会渲染：
   - `结果引用`
   - 阶段总结区间信息
   - `预览结果` 按钮
2. 点击预览按钮后：
   - 会调用 [`previewWorkflowFile()`](../novel_agent/web/static/app-core.js:703)
   - 并使用实际阶段总结路径作为参数

**测试结果**：

```bash
npx vitest run ./frontend-tests/copilot-workflow.dom.test.js
1 passed / 5 tests passed
```

## 四点十二、第十三阶段新增完成项：多类型任务产物统一预览

### ✅ 世界观 / 大纲 / 章节正文 / 阶段总结统一结果标签
**状态：已完成**

**实施位置**：

- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:426)

**已落地内容**：

在主工作区任务池页面中，结果引用展示已从“仅阶段总结可读”扩展为对多类项目级任务统一识别：

- `build_world` → 世界观设定
- `build_outline` → 项目大纲
- `write_chapter` → 章节正文
- `summary_orchestrate` → 阶段总结

当前逻辑会优先复用已有：

- `result_kind`
- `result_ref`

并在缺失 `result_kind` 时基于 `task_type` 自动推断展示标签。

**实现效果**：

- 正式任务池中的核心项目级产物已具备统一可读标签
- 用户可以直接区分世界观、大纲、正文与阶段总结产物
- 结果展示从“路径可见”进一步推进到“语义可辨识”

### ✅ 多类型结果文件统一预览
**状态：已完成**

**实施位置**：

- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:426)
- [`previewWorkflowFile()`](../novel_agent/web/static/app-core.js:703)

**已落地内容**：

当前任务池页“预览结果”按钮已不再只服务于阶段总结，而是统一覆盖：

- `worldbuilding.json`
- `outline.json`
- `chapters/*.md`
- `stage_summaries/*.md`

其中章节任务会自动展示章节号标签，阶段总结继续展示章节区间标签。

**实现效果**：

- 任务池页面已真正成为项目级核心产物的统一预览入口
- 用户无需离开任务池页面即可查看世界观 / 大纲 / 正文 / 阶段总结
- 多类型任务结果开始共享同一套前端协作可视化能力

### ✅ 第十三阶段前端 DOM 测试补齐
**状态：已完成**

**实施位置**：

- [`frontend-tests/copilot-workflow.dom.test.js`](../frontend-tests/copilot-workflow.dom.test.js)

**新增测试范围**：

本轮新增并通过了多类型结果预览断言：

1. 打开任务池工作区后会渲染：
   - 世界观设定
   - 项目大纲
   - 章节正文
   - 阶段总结
2. 点击各任务的“预览结果”按钮后：
   - 会分别调用 [`previewWorkflowFile()`](../novel_agent/web/static/app-core.js:703)
   - 参数覆盖：
     - `worldbuilding.json`
     - `outline.json`
     - `chapters/001-第一章.md`
     - `stage_summaries/第1-10章-剧情总结.md`

**测试结果**：

```bash
npx vitest run ./frontend-tests/copilot-workflow.dom.test.js
1 passed / 5 tests passed
```

---

## 四点十三、第十四阶段新增完成项：任务详情抽屉与监督摘要可视化

### ✅ 任务详情抽屉 / 弹层
**状态：已完成**

**实施位置**：

- [`showCollabTaskDetail()`](../novel_agent/web/static/continuous_write.js:481)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:555)

**已落地内容**：

本轮为主工作区任务池中的每个任务卡片增加了：

- `查看详情` 按钮

点击后会打开统一任务详情弹层，当前已展示：

- 任务标题 / `task_type`
- 当前状态
- 重试次数
- 认领 Agent
- 候选 Agent
- 依赖任务
- 输入参数
- 运行态元数据
- `result_ref`

**实现效果**：

- 任务池页面已不再停留在卡片级摘要
- 用户可以直接查看任务运行细节而不必切出页面
- 为后续补充任务级恢复锚点、失败原因和重试控制打下 UI 基础

### ✅ 项目级停止信息摘要卡片
**状态：已完成**

**实施位置**：

- [`refreshNovelCollabRuntime()`](../novel_agent/web/static/app-core.js:564)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:555)

**已落地内容**：

前端运行态已正式消费状态接口中的：

- `project_ready_execution`

并在任务池主工作区新增“项目级调度停止信息”卡片，当前展示：

- `stop_reason`
- `stopped_on_task_type`
- `chapter_tasks_executed`
- `executed_task_count`
- `max_tasks`
- `max_chapter_tasks`
- `updated_at`

其中停止原因已做前端语义映射，如：

- `max_chapter_tasks_reached` → 达到连续章节上限
- `fallback_triggered` → 触发回退停止
- `review_required` → 触发复核停止

**实现效果**：

- 前端已可直接读懂“为什么这一轮停下”
- 项目级监督信息从接口字段提升为页面级可视化摘要
- 停止点、批次上限与章节推进统计已开始成为可观察运行态的一部分

### ✅ 下一批 ready-task 候选信息展示
**状态：已完成**

**实施位置**：

- [`getCollabNextReadyTasks()`](../novel_agent/web/static/continuous_write.js:461)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:555)

**已落地内容**：

本轮在前端基于当前任务池状态新增了下一批 ready-task 候选推导逻辑：

1. 收集 `status = completed` 的任务 ID
2. 在 `pending` 任务中筛选：
   - 所有 `depends_on` 均已满足
3. 在右侧栏以摘要卡片形式展示最多前 4 个候选

当前候选卡片会展示：

- 任务标题
- `task_type`
- 候选 Agent

**实现效果**：

- 用户已可直接预判下一轮最可能被继续调度的任务
- 主工作区开始具备“当前停在哪 / 接下来会做什么”的最小解释能力
- 为后续继续扩展恢复入口与手动推进能力提供了前端锚点

### ✅ 第十四阶段前端 DOM 测试补齐
**状态：已完成**

**实施位置**：

- [`frontend-tests/copilot-workflow.dom.test.js`](../frontend-tests/copilot-workflow.dom.test.js)

**新增测试范围**：

本轮新增并通过了以下断言：

1. 打开任务池工作区后会渲染：
   - 项目级调度停止信息
   - `达到连续章节上限`
   - 下一批 ready-task 候选
   - 候选任务标题
2. 点击任务卡片的 `查看详情` 按钮后：
   - 会打开任务详情弹层
   - 会展示候选 Agent / 结果引用等详情
3. 轮询重渲染断言同步更新：
   - [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:555) 第三个参数会透传 `projectReadyExecution`

**测试结果**：

```bash
npx vitest run ./frontend-tests/copilot-workflow.dom.test.js
1 passed / 5 tests passed
```

## 四点十四、第十五阶段新增完成项：协作轨迹时间线过滤增强

### ✅ 协作轨迹时间线阶段过滤
**状态：已完成**

**实施位置**：

- [`getCollabTraceStageMeta()`](../novel_agent/web/static/continuous_write.js:393)
- [`buildCollabTraceFilterOptions()`](../novel_agent/web/static/continuous_write.js:427)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:614)

**已落地内容**：

本轮在主工作区“协作轨迹时间线”区域中新增了阶段过滤能力。由于后端事件当前没有统一持久化 `stage` 字段，前端改为基于现有稳定字段做阶段归类映射，当前已支持：

- `contract_confirmation / contract_rejection` → 合同/初始化
- `build_world` → 世界观构建
- `build_outline` → 大纲生成
- `write_chapter` → 章节写作
- `summary_orchestrate` → 阶段总结
- `context_plan` / `content_read` / `evaluate_chapter` / `polish_chapter` / `expand_content` → 各自章节子阶段
- `project_ready_execution_cycle` → 项目调度

并在右侧时间线面板增加：

- `阶段过滤` 下拉框
- 阶段标签文本展示

**实现效果**：

- 时间线不再只能按时间顺序平铺查看
- 用户已可聚焦某一类阶段事件，快速定位合同确认、项目调度、章节写作或阶段总结链路
- 在不改后端事件协议的前提下，前端先完成了“阶段视角观察”的最小闭环

### ✅ 协作轨迹事件类型过滤与空结果提示
**状态：已完成**

**实施位置**：

- [`getCollabTraceEventTypeLabel()`](../novel_agent/web/static/continuous_write.js:385)
- [`filterCollabExecutionEvents()`](../novel_agent/web/static/continuous_write.js:443)
- [`renderCollabExecutionTimeline()`](../novel_agent/web/static/continuous_write.js:460)

**已落地内容**：

在阶段过滤之外，当前时间线还新增了事件类型过滤能力，直接基于事件自身的 `type` 字段进行筛选，当前已覆盖：

- `contract_confirmation`
- `task_registered`
- `task_started`
- `task_completed`
- `task_failed`
- `task_fallback_started`
- `project_ready_execution_cycle`

同时为常见类型补充了前端语义标签，如：

- `task_started` → 任务开始
- `task_completed` → 任务完成
- `project_ready_execution_cycle` → 项目调度批次

当阶段过滤与事件类型过滤组合后没有匹配项时，页面会显示：

- “当前筛选条件下没有匹配事件，请切换阶段或事件类型后重试。”

**实现效果**：

- 用户已可从“阶段”和“事件类型”两个维度交叉查看协作轨迹
- 时间线开始具备更接近调试面板的筛选体验
- 对于空结果场景已不再沉默，避免误以为时间线没有刷新

### ✅ 第十五阶段前端 DOM 测试补齐
**状态：已完成**

**实施位置**：

- [`frontend-tests/copilot-workflow.dom.test.js`](../frontend-tests/copilot-workflow.dom.test.js)

**新增测试范围**：

本轮新增并通过了以下断言：

1. 打开任务池工作区后会渲染：
   - `阶段过滤`
   - `事件类型过滤`
   - 合同/初始化、项目调度等过滤选项
2. 切换阶段过滤后：
   - 时间线仅保留对应阶段事件
3. 切换事件类型过滤后：
   - 时间线仅保留对应事件类型
4. 使用“合法选项但无交集”的组合过滤时：
   - 会展示“当前筛选条件下没有匹配事件”提示

**测试结果**：

```bash
npx vitest run ./frontend-tests/copilot-workflow.dom.test.js
1 passed / 5 tests passed
```

## 四点十五、第十六阶段新增完成项：协作轨迹时间线过滤后二级能力增强

### ✅ 阶段统计 / 事件类型统计
**状态：已完成**

**实施位置**：

- [`buildCollabTraceStats()`](../novel_agent/web/static/continuous_write.js:504)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:709)

**已落地内容**：

在主工作区“协作轨迹时间线”右侧面板中，当前过滤结果已新增二级统计区，直接基于过滤后的事件集实时计算并展示：

1. 阶段统计：
   - 合同/初始化
   - 世界观构建
   - 大纲生成
   - 章节写作
   - 阶段总结
   - 项目调度
   - 其他章节子阶段
2. 事件类型统计：
   - 合同确认
   - 任务开始
   - 任务完成
   - 任务失败
   - 回退启动
   - 项目调度批次

同时统计区已随着过滤条件切换实时刷新，而不是只展示原始全量事件计数。

**实现效果**：

- 时间线过滤结果不再只是“剩下几条卡片”
- 用户可以直接看到当前过滤结果的结构分布
- 协作轨迹开始具备更接近“运行态观察面板”的统计视角

### ✅ 最近一次匹配事件摘要与高亮
**状态：已完成**

**实施位置**：

- [`renderCollabExecutionTimeline()`](../novel_agent/web/static/continuous_write.js:523)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:709)

**已落地内容**：

本轮在过滤结果基础上补充了“最近一次匹配事件”的双层可视化：

1. 在时间线卡片列表中：
   - 为最新一条匹配事件增加 `is-latest-match`
   - 增加 `最近匹配` badge
   - 对卡片背景与边框做高亮
2. 在时间线过滤区上方：
   - 新增“最近匹配事件”摘要卡片
   - 展示事件标题
   - 展示阶段与事件类型
   - 展示时间戳
3. 当当前过滤结果为空时：
   - 会明确展示“当前过滤结果暂无最近匹配事件”

**实现效果**：

- 用户无需手动在过滤结果中判断“最新一条是哪条”
- 最新匹配事件已经从隐式时间顺序变成显式焦点
- 协作轨迹对调试、巡检和审查更友好

### ✅ 过滤条件持久化与重渲染保持
**状态：已完成**

**实施位置**：

- [`multiAgentWriteState`](../novel_agent/web/static/continuous_write.js:322)
- [`getCollabTraceFilters()`](../novel_agent/web/static/continuous_write.js:466)
- [`setCollabTraceFilters()`](../novel_agent/web/static/continuous_write.js:477)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:709)

**已落地内容**：

此前时间线过滤状态只存在于页面渲染函数局部变量中，一旦任务池页刷新、轮询重渲染或手动再次打开工作区，过滤条件就会回落到默认值。
本轮已将过滤状态提升到前端协作页面状态中统一管理：

- `multiAgentWriteState.collabTraceFilters.stage`
- `multiAgentWriteState.collabTraceFilters.type`

并完成：

1. 下拉框初始值回填
2. 过滤条件变更时写回共享状态
3. 再次调用 [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:709) 后仍保持原有筛选值
4. 时间线摘要中的“当前匹配数”也会随重渲染同步刷新

**实现效果**：

- 前端轮询刷新不再打断用户当前筛选上下文
- 任务池页重渲染后仍能保持同一观察视角
- 时间线过滤第一次具备了“连续使用”的状态记忆能力

### ✅ 第十六阶段前端 DOM 测试补齐
**状态：已完成**

**实施位置**：

- [`frontend-tests/copilot-workflow.dom.test.js`](../frontend-tests/copilot-workflow.dom.test.js)

**新增测试范围**：

本轮新增并通过了以下断言：

1. 打开任务池工作区后会渲染：
   - `阶段统计`
   - `事件类型统计`
   - `最近匹配`
2. 切换到某一阶段后：
   - 统计区只反映当前过滤结果
   - 最新匹配事件卡片会带高亮类名
   - 顶部摘要会显示正确的“当前匹配数”
3. 过滤无结果时：
   - 会显示“当前过滤结果暂无最近匹配事件”
4. 再次调用 [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:709) 重渲染后：
   - 过滤条件仍会保持
   - 最近匹配摘要仍与过滤状态一致

**测试结果**：

```bash
npx vitest run ./frontend-tests/copilot-workflow.dom.test.js
5 passed
```

## 四点十六、第十七阶段新增完成项：协作轨迹时间线过滤后三级能力增强

### ✅ 统计排序与观察优先级优化
**状态：已完成**

**实施位置**：

- [`buildCollabTraceStats()`](../novel_agent/web/static/continuous_write.js:609)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:849)

**已落地内容**：

本轮将时间线过滤后的统计结果从“按首次出现顺序展示”改为“按频次优先展示”，并补上稳定排序规则：

1. 阶段统计按数量降序排列
2. 事件类型统计按数量降序排列
3. 当数量相同时再按中文标签做稳定排序

**实现效果**：

- 高频阶段 / 高频事件会优先暴露在观察面板前列
- 过滤结果的主导结构更容易被第一时间识别
- 统计区开始从“补充信息”向“观察入口”演进

### ✅ 最近匹配事件跳转定位
**状态：已完成**

**实施位置**：

- [`renderCollabExecutionTimeline()`](../novel_agent/web/static/continuous_write.js:639)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:1030)

**已落地内容**：

本轮继续增强“最近匹配事件”的可操作性：

1. 为每个时间线事件卡片补充稳定 DOM 锚点
2. 当过滤条件切换后，若存在最近匹配事件：
   - 自动定位对应卡片
   - 调用滚动定位能力
3. 定位过程兼容前端测试环境中的 `scrollIntoView` mock

**实现效果**：

- 用户切换过滤后不必再手动在长列表中寻找焦点事件
- “最近匹配”从摘要高亮扩展为直接定位
- 时间线的巡检效率进一步提升

### ✅ 常用过滤组合快捷入口
**状态：已完成**

**实施位置**：

- [`buildCollabTraceQuickFilters()`](../novel_agent/web/static/continuous_write.js:554)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:849)

**已落地内容**：

在阶段/事件类型下拉过滤之外，右侧面板新增常用过滤组合快捷入口，当前会按事件集动态提供：

- `查看全部`
- `只看阶段总结`
- `只看项目调度`
- `只看任务完成`
- `只看任务失败`

点击后会直接写回共享过滤状态并触发时间线重渲染。

**实现效果**：

- 高频观察场景已不必每次手动组合两个下拉框
- 时间线过滤首次具备“预设视角”能力
- 为后续继续扩展命名过滤模板打下交互基础

### ✅ 过滤条件本地持久化（跨页面刷新）
**状态：已完成**

**实施位置**：

- [`COLLAB_TRACE_FILTERS_STORAGE_KEY`](../novel_agent/web/static/continuous_write.js:322)
- [`loadCollabTraceFiltersFromStorage()`](../novel_agent/web/static/continuous_write.js:469)
- [`saveCollabTraceFiltersToStorage()`](../novel_agent/web/static/continuous_write.js:489)
- [`getCollabTraceFilters()`](../novel_agent/web/static/continuous_write.js:500)

**已落地内容**：

在上一阶段“重渲染保持”的基础上，本轮进一步把过滤条件提升为本地持久化状态：

1. 过滤条件会写入 `localStorage`
2. 再次打开任务池工作区时会自动恢复：
   - `stage`
   - `type`
3. 页面刷新后仍可保持原有过滤视角

**实现效果**：

- 用户的协作轨迹观察上下文不再局限于单次渲染周期
- 时间线已具备跨页面刷新恢复的最小记忆能力
- 这为后续继续扩展命名过滤模板提供了直接状态锚点

### ✅ 第十七阶段前端 DOM 测试补齐
**状态：已完成**

**实施位置**：

- [`frontend-tests/copilot-workflow.dom.test.js`](../frontend-tests/copilot-workflow.dom.test.js)

**新增测试范围**：

本轮新增并通过了以下断言：

1. 统计区结果按数量优先排序
2. 切换过滤后会定位最近匹配事件
3. 快捷过滤组合可直接驱动时间线重渲染
4. 再次渲染任务池工作区后，本地持久化过滤条件仍会恢复

**测试结果**：

```bash
npx vitest run ./frontend-tests/copilot-workflow.dom.test.js
5 passed
```

## 四点十七、第十八阶段新增完成项：协作轨迹时间线过滤后四级能力增强

### ✅ 可视化筛选面包屑
**状态：已完成**

**实施位置**：

- [`renderCollabTraceFilterBreadcrumbs()`](../novel_agent/web/static/continuous_write.js:639)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:1030)

**已落地内容**：

本轮在协作轨迹时间线过滤区中新增可视化筛选面包屑，当前会把实时过滤状态显式渲染为独立徽标：

1. 固定展示 `当前筛选`
2. 以独立面包屑展示：
   - `阶段`
   - `事件`
3. 当未筛选时显示：
   - `全部阶段`
   - `全部事件`
4. 当切换阶段或事件类型后，面包屑会即时同步更新

**实现效果**：

- 当前观察视角已不再只隐含在下拉框值中
- 用户可以更直观识别自己正在查看的过滤组合
- 时间线过滤开始具备更接近专业运行态面板的“筛选上下文显式化”能力

### ✅ 事件卡片详情展开 / 收起
**状态：已完成**

**实施位置**：

- [`getCollabTraceExpandedEventIds()`](../novel_agent/web/static/continuous_write.js:529)
- [`toggleCollabTraceExpandedEventId()`](../novel_agent/web/static/continuous_write.js:535)
- [`clearCollabTraceExpandedEventIds()`](../novel_agent/web/static/continuous_write.js:550)
- [`renderCollabExecutionTimeline()`](../novel_agent/web/static/continuous_write.js:639)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:1203)

**已落地内容**：

本轮为每张时间线事件卡片补充了详情展开能力：

1. 事件卡片新增：
   - `展开详情`
   - `收起详情`
2. 展开后会在卡片内渲染：
   - 事件类型
   - 标题
   - `task_type`
   - `task_id`
   - `agent`
   - `status`
   - 时间戳
   - 阶段标签
3. 展开状态统一保存在：
   - `multiAgentWriteState.collabTraceExpandedEventIds`
4. 当过滤条件切换时，会自动清空展开态，避免旧事件上下文残留到新的过滤结果中

**实现效果**：

- 协作轨迹已不再只能查看卡片级摘要
- 用户可以在不离开时间线的情况下查看事件明细载荷
- 过滤切换与展开态清理形成一致的上下文边界，减少误读风险

### ✅ 第十八阶段前端 DOM 测试补齐
**状态：已完成**

**实施位置**：

- [`frontend-tests/copilot-workflow.dom.test.js`](../frontend-tests/copilot-workflow.dom.test.js)

**新增测试范围**：

本轮新增并通过了以下断言：

1. 打开任务池工作区后会渲染：
   - `当前筛选`
   - `全部阶段`
   - `全部事件`
2. 切换到特定阶段后：
   - 面包屑会同步显示当前阶段标签
3. 点击时间线卡片的 `展开详情` 后：
   - 会显示 `事件详情`
   - 会展示对应事件的 `task_type`
4. 再次点击 `收起详情` 后：
   - 详情区会被移除
5. 过滤条件切换后：
   - `collabTraceExpandedEventIds` 会被清空
   - 面包屑与时间线结果保持一致

**测试结果**：

```bash
npx vitest run ./frontend-tests/copilot-workflow.dom.test.js
5 passed
```

## 四点十八、第十九阶段新增完成项：协作轨迹时间线过滤后五级能力增强

### ✅ 命名过滤模板保存
**状态：已完成**

**实施位置**：

- [`COLLAB_TRACE_NAMED_FILTERS_STORAGE_KEY`](../novel_agent/web/static/continuous_write.js:323)
- [`saveCollabTraceNamedFilter()`](../novel_agent/web/static/continuous_write.js:543)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:878)

**已落地内容**：

本轮在协作轨迹时间线右侧面板新增“命名过滤模板”首批闭环，用户现在可以把当前过滤组合保存为自定义模板：

1. 新增模板存储键：
   - `collab_trace_named_filters`
2. 新增模板保存逻辑：
   - 输入模板名称
   - 保存当前 `stage/type` 过滤组合
3. 若模板名称重复，则直接覆盖更新
4. 模板信息会记录：
   - `id`
   - `name`
   - `filters`
   - `updated_at`

**实现效果**：

- 协作轨迹过滤首次具备“用户自定义模板”能力
- 常用观察视角不再只能依赖固定快捷过滤
- 为后续继续扩展模板重命名、排序和分享打下状态基础

### ✅ 命名过滤模板应用 / 删除
**状态：已完成**

**实施位置**：

- [`getCollabTraceNamedFilters()`](../novel_agent/web/static/continuous_write.js:539)
- [`deleteCollabTraceNamedFilter()`](../novel_agent/web/static/continuous_write.js:567)
- [`renderCollabTraceNamedFilters()`](../novel_agent/web/static/continuous_write.js:577)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:878)

**已落地内容**：

当前每个模板卡片已提供：

- `应用`
- `删除`

点击 `应用` 后会：

1. 把模板中的过滤组合写回当前过滤状态
2. 同步回填阶段 / 事件类型下拉框
3. 清空旧的展开态
4. 触发时间线重渲染

点击 `删除` 后会：

1. 从本地模板存储移除对应模板
2. 立即刷新模板列表区

**实现效果**：

- 过滤模板已从“可保存”推进到“可真正复用”
- 用户可在不同观察任务间快速切换固定视角
- 模板生命周期已形成最小闭环：保存 → 应用 → 删除

### ✅ 第十九阶段前端 DOM 测试补齐
**状态：已完成**

**实施位置**：

- [`frontend-tests/copilot-workflow.dom.test.js`](../frontend-tests/copilot-workflow.dom.test.js)

**新增测试范围**：

本轮新增并通过了以下断言：

1. 打开任务池工作区后会渲染：
   - `命名过滤模板`
   - `保存当前过滤`
2. 输入模板名称并保存后：
   - 页面会展示模板卡片
   - `localStorage` 会写入模板数据
3. 点击模板 `应用` 后：
   - 会恢复对应过滤条件
   - 面包屑会同步显示模板过滤结果
4. 点击模板 `删除` 后：
   - 模板卡片会从页面移除

**测试结果**：

```bash
npx vitest run ./frontend-tests/copilot-workflow.dom.test.js
5 passed
```

## 四点十九、第二十阶段新增完成项：协作轨迹命名过滤模板重命名 / 排序首批闭环

### ✅ 命名过滤模板重命名
**状态：已完成**

**实施位置**：

- [`renameCollabTraceNamedFilter()`](../novel_agent/web/static/continuous_write.js:577)
- [`renderCollabTraceNamedFilters()`](../novel_agent/web/static/continuous_write.js:628)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:1431)

**已落地内容**：

本轮在已有命名过滤模板“保存 / 应用 / 删除”闭环基础上，继续补齐模板可维护能力：

1. 新增 [`renameCollabTraceNamedFilter()`](../novel_agent/web/static/continuous_write.js:577)
2. 支持按模板 `id` 原地更新模板名称
3. 保留原有：
   - `filters`
   - `id`
4. 重命名时同步刷新：
   - `updated_at`
5. 当目标名称与其他模板重复时：
   - 返回冲突信号
   - 前端提示使用其他名称
6. 模板卡片新增：
   - `重命名` 按钮
   - `最近更新` 展示

**实现效果**：

- 命名模板已不再只能“删除后重建”
- 用户可直接维护既有观察模板
- 模板能力从“可保存”继续推进到“可编辑”

### ✅ 命名过滤模板排序（上移 / 下移）
**状态：已完成**

**实施位置**：

- [`moveCollabTraceNamedFilter()`](../novel_agent/web/static/continuous_write.js:599)
- [`renderCollabTraceNamedFilters()`](../novel_agent/web/static/continuous_write.js:628)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:1431)

**已落地内容**：

本轮继续补齐命名模板的最小排序管理能力，当前已支持：

1. `上移`
2. `下移`

排序逻辑特征：

- 基于当前模板数组顺序进行相邻交换
- 边界位置不会产生越界移动
- 调整后立即写回本地模板存储
- 模板列表会即时重渲染

**实现效果**：

- 高频使用模板可以被调到更靠前位置
- 模板列表开始具备最小“整理能力”
- 为后续扩展置顶、分组、批量整理提供了直接基础

### ✅ 第二十阶段前端 DOM 测试补齐
**状态：已完成**

**实施位置**：

- [`frontend-tests/copilot-workflow.dom.test.js`](../frontend-tests/copilot-workflow.dom.test.js)

**新增测试范围**：

本轮新增并通过了以下断言：

1. 命名过滤模板可按目标卡片触发重命名
2. 重命名结果会写回 `localStorage`
3. 模板可按当前顺序执行有效方向移动
4. 排序后模板顺序会发生变化
5. 反向移动后模板顺序可恢复
6. 重命名后的模板仍可继续：
   - 应用
   - 删除

**测试结果**：

```bash
npx vitest run ./frontend-tests/copilot-workflow.dom.test.js
5 passed
```

## 四、当前阶段判断

按照原方案第 10 节的分阶段规划，当前实施状态可判断为：

### 阶段一：协议化现有强编排
**状态：已完成**

已完成标志：

- 已有 `CreationContract`
- 已有 `TaskDefinition`
- 已能生成合同与任务图草案
- 章节链已能结构化表达为任务对象

### 阶段二：能力声明化
**状态：已完成**

已完成标志：

- 已有统一能力声明接口
- 已有能力注册表
- 任意任务可通过能力表查询候选 Agent

### 阶段三：部分自治试点
**状态：已完成，并进入任务市场化深化**

已完成标志：

- 章节内部链已接入最小自治执行入口
- 已实现候选查询与执行记录
- 已实现监督式 fallback 回退
- 已实现项目级 `task_pool` 持续写回
- 已实现项目级 `collab_execution_trace` 持续事件追加
- 已补专项测试验证

### 阶段四：讨论 → 合同 → 自治执行闭环
**状态：已完成第一轮闭环**

已完成标志：

1. 讨论阶段已能返回待确认合同草案
2. 已增加合同确认接口
3. 用户确认后可自动初始化正式任务池
4. 已落盘 `task_pool` 与 `collab_execution_trace`
5. Copilot 已具备合同确认与任务池摘要入口
6. 正式任务池已能在章节自治执行过程中持续更新状态
7. 协作轨迹已能持续追加运行事件

### 阶段五：章节级最小任务市场循环试点
**状态：已完成并稳定运行**

已完成标志：

1. 章节内部已新增章节级 `TaskPool`
2. `ContextStrategy -> ContentReader` 已改为依赖触发式推进
3. `Evaluator -> Polisher` 已接入最小评估回流逻辑
4. `expand_content` 已可衔接修订后结果
5. `summary_orchestrate` 已接入同一章节级任务市场循环
6. 已补专项后端测试并通过

### 阶段六：项目级正式任务池最小调度试点
**状态：已完成**

已完成标志：

1. 正式任务池初始化后已可自动执行 `build_world`
2. 正式任务池初始化后已可自动执行 `build_outline`
3. 合同语义依赖已映射为真实 `depends_on`
4. 合同确认接口已自动拉起首批项目级 ready-task

### 阶段七：项目级正式 `write_chapter` 接入任务池
**状态：已完成**

已完成标志：

1. 项目级 `write_chapter` 已接入正式任务池支持集
2. 已补齐章节标题/大纲/前序章节输入装配
3. 已补齐项目级章节结果落盘与 `result_ref`
4. 项目级任务池已可推进到真实正文创作

### 阶段八：项目级多章节连续调度试点
**状态：已完成第一轮试点**

已完成标志：

1. 项目级 ready-task 调度已支持连续推进多个 `write_chapter`
2. 已新增章节连续执行上限 `max_chapter_tasks`
3. 已新增停止原因字段 `stop_reason`
4. 已返回 `chapter_tasks_executed` 作为本轮执行统计
5. 合同确认后已能自动执行“小批量连续章节试点”
6. 已补专项后端测试并通过

### 阶段九：项目级监督能力增强试点
**状态：已完成第一轮试点**

已完成标志：

1. 项目级停止原因已写回 `task_pool.metadata.project_ready_execution`
2. 协作轨迹已追加 `project_ready_execution_cycle`
3. 项目级调度已支持 `fallback_triggered`
4. 项目级调度已支持显式 opt-in 的 `review_required`
5. 状态接口已直接透出 `project_ready_execution`
6. 已补专项后端测试并通过

### 阶段十：项目级正式 `summary_orchestrate` 接入任务池
**状态：已完成第一轮试点**

已完成标志：

1. 默认合同任务图已自动纳入项目级 `summary_orchestrate`
2. 项目级依赖映射已把阶段总结绑定到对应 `write_chapter` 完成
3. 项目级调度已可真实执行阶段总结任务
4. 阶段总结结果已写回 `collab_stage_summaries`
5. 阶段总结文件已落盘到 `stage_summaries/*.md`
6. 正式任务池 `result_ref` 已指向阶段总结产物
7. 已补专项后端测试并通过

### 阶段十一：任务池结果引用与文件预览入口
**状态：已完成第一轮试点**

已完成标志：

1. 主工作区任务池已展示任务级 `result_ref`
2. 阶段总结任务已展示 `summary_path` / `summary_range`
3. 任务池页面已可直接预览阶段总结文件
4. 已补前端 DOM 测试并通过

### 阶段十二：多类型任务产物统一预览
**状态：已完成第一轮试点**

已完成标志：

1. 主工作区任务池已统一识别世界观 / 大纲 / 章节正文 / 阶段总结四类产物
2. 多类型任务产物已接入同一套“预览结果”入口
3. 已补前端 DOM 测试并通过

### 阶段十三：任务详情抽屉与监督摘要可视化
**状态：已完成第一轮试点**

已完成标志：

1. 主工作区任务池已增加任务详情弹层 / 抽屉入口
2. 前端已展示 `project_ready_execution` 停止信息摘要
3. 右侧栏已展示下一批 ready-task 候选信息
4. 已补前端 DOM 测试并通过

### 阶段十四：协作轨迹时间线过滤增强
**状态：已完成第一轮试点**

已完成标志：

1. 协作轨迹时间线已增加“阶段过滤”下拉
2. 协作轨迹时间线已增加“事件类型过滤”下拉
3. 前端已建立基于 `task_type` / 特殊事件类型的阶段归类映射
4. 过滤无匹配时已展示显式空结果提示
5. 已补前端 DOM 测试并通过

### 阶段十五：协作轨迹时间线过滤后二级能力增强
**状态：已完成第一轮试点**

已完成标志：

1. 过滤结果已增加阶段统计
2. 过滤结果已增加事件类型统计
3. 最近一次匹配事件已支持摘要展示与卡片高亮
4. 过滤条件已在任务池页重渲染后保持
5. 已补前端 DOM 测试并通过

### 阶段十六：协作轨迹时间线过滤后三级能力增强
**状态：已完成第一轮试点**

已完成标志：

1. 统计结果已按数量优先排序
2. 最近匹配事件已支持自动跳转定位
3. 协作轨迹页已增加常用过滤组合快捷入口
4. 过滤条件已支持本地持久化并在页面刷新后恢复
5. 已补前端 DOM 测试并通过

### 阶段十七：协作轨迹时间线过滤后四级能力增强（首批闭环）
**状态：已完成第一轮试点**

已完成标志：

1. 协作轨迹页已增加可视化筛选面包屑
2. 时间线事件卡片已支持详情展开 / 收起
3. 展开态已纳入共享页面状态统一管理
4. 过滤切换后会自动清空旧展开态
5. 已补前端 DOM 测试并通过

尚未完全落地的点包括：

1. 项目级调度当前仍由 Coordinator 内部循环推进，不是独立调度器/仲裁器
2. `stop_reason` 目前仍未覆盖预算超限、冲突仲裁、超时等复杂停机原因
3. 项目级连续调度仍偏串行，尚未进入并发预算控制
4. 依赖推进仍未切换到消息总线驱动的候选认领/协商机制
5. Coordinator 仍承担较多执行细节，尚未完全收敛为监督/仲裁角色

---

## 五、下一阶段建议实施顺序

建议直接按以下顺序进入下一轮开发。

### 第一优先级：项目级正式任务池继续扩容
1. 为更多项目级任务补 `result_ref`、失败原因、恢复锚点与重试信息
2. 让正式任务池从“可连续推进多章”继续走向“可恢复、可审计”
3. 为停止条件继续补充：
   - `budget_exceeded`
   - `conflict_detected`
   - `timeout_reached`
   - `user_confirmation_required`
4. 继续把更多项目级收尾/治理任务纳入正式 `task_pool`
5. 将任务池结果预览从阶段总结扩展到章节正文 / 世界观 / 大纲等更多产物类型

### 第一优先级补充说明：本轮已完成项
1. 已将 `summary_orchestrate`、阶段总结落盘等项目级收尾任务纳入正式 `task_pool`
2. 已为阶段总结任务补齐 `result_ref` 与稳定文件锚点
3. 正式任务池已进一步从“可连续推进多章”扩展到“可产出阶段总结收尾结果”
4. 主工作区任务池已补齐阶段总结结果引用展示与文件预览入口
5. 任务池结果预览已扩展到章节正文 / 世界观 / 大纲等更多产物类型

### 第二优先级：项目级调度器监督能力增强
1. 在项目级 ready-task 循环中增加预算、超时、冲突仲裁字段
2. 让停止原因继续扩展到更多监督策略，而不只限于 fallback 与显式 review
3. 为连续调度补更明确的恢复入口与恢复策略
4. 逐步把 Coordinator 从执行者收敛为监督者 / 收敛控制器

### 第三优先级：协议协作深化
1. 让部分任务真正通过消息协议驱动，而不只是 Coordinator 内部循环
2. 优先试点：
   - `ContextStrategy`
   - `ContentReader`
   - `Evaluator`
   - `Polisher`
   - `SummaryOrchestrator`
3. 逐步把“依赖触发”推进为“协议驱动的候选认领与结果提交”

### 第四优先级：前端协作可视化继续增强
1. 将合同视图从 Copilot 卡片提升为独立主视图
2. 在任务池页面补任务详情抽屉后的二级能力：
   - 失败原因
   - 恢复锚点
   - 重试信息
   - 结果引用扩展字段
3. 在协作轨迹页继续增强过滤后的更高阶能力：
   - 模板重命名 / 排序
   - 阶段分组 / 折叠视图
   - 时间线详情字段按类型差异化展示
   - 模板导入 / 导出
4. 在前端继续增强：
   - `stop_reason` 的更多监督策略映射
   - 连续执行章节数与当前批次停止点的强调展示
   - 下一 ready-task 候选的更多上下文信息
   - 手动恢复 / 手动推进入口

### 第四优先级补充说明：本轮已完成项
1. 协作轨迹页已补齐阶段统计与事件类型统计
2. 协作轨迹页已补齐最近一次匹配事件摘要与卡片高亮
3. 协作轨迹页已补齐统计排序、最近匹配跳转定位与快捷过滤入口
4. 过滤条件已支持本地持久化并在页面刷新后恢复
5. 协作轨迹页已补齐可视化筛选面包屑
6. 时间线事件卡片已补齐详情展开 / 收起
7. 已补齐命名过滤模板保存 / 应用 / 删除
8. 已补前端 DOM 回归测试并通过

---

## 六、总体结论

本轮改造已经把原方案中的“第一批开发任务”、第二阶段“合同确认闭环”、第三阶段“正式任务池运行态深化”、第四阶段“主工作区任务池页面化 / 协作轨迹时间线 / 主动轮询 / DOM 测试”、第五阶段“章节级最小任务市场循环 / 依赖触发 / 评估回流 / 后端专项测试”，以及后续“项目级正式任务池 ready-task 调度 / 正式 `write_chapter` 接入 / 多章节连续调度与停止条件显式化 / 项目级停止原因写回与状态接口透出 / 项目级正式 `summary_orchestrate` 接入与阶段总结落盘 / 任务池结果引用展示与阶段总结文件预览入口 / 世界观、大纲、章节正文、阶段总结多类型产物统一预览 / 任务详情抽屉 / 项目级停止信息摘要 / 下一 ready-task 候选可视化 / 协作轨迹时间线阶段过滤与事件类型过滤 / 协作轨迹过滤结果的统计、最近匹配高亮与过滤条件保持 / 统计排序 / 最近匹配事件跳转定位 / 常用过滤组合快捷入口 / 过滤条件本地持久化 / 可视化筛选面包屑 / 事件卡片详情展开 / 命名过滤模板保存 / 应用 / 删除”推进到可运行、可测试、可回退的状态。

当前项目已经从：

- “强编排链路 + 少量多 Agent 包装”

演进到：

- “**带合同、带能力声明、带正式任务池初始化、带运行态任务池写回、带协作轨迹持续追加、带主工作区任务池页面与时间线、带前端主动轮询、带章节级最小任务市场循环、带依赖触发与评估回流、带项目级 ready-task 调度、带正式 `write_chapter` 接入、带项目级正式 `summary_orchestrate` 接入、带阶段总结结果落盘与 `result_ref`、带任务池结果引用展示与阶段总结文件预览入口、带世界观 / 大纲 / 章节正文 / 阶段总结多类型产物统一预览、带任务详情抽屉、带项目级停止信息摘要、带下一 ready-task 候选信息、带协作轨迹时间线阶段过滤与事件类型过滤、带过滤结果统计、带最近匹配事件高亮、带统计排序、带最近匹配事件跳转定位、带常用过滤组合快捷入口、带过滤条件本地持久化、带可视化筛选面包屑、带事件卡片详情展开、带命名过滤模板保存 / 应用 / 删除、带多章节连续执行上限与停止原因、带项目级停止原因回写、带状态接口透出监督信息、带回退策略、带 Copilot 可视化确认入口的监督式自组织过渡态**”

这意味着后续不需要再从零开始设计底层框架，而可以直接进入：

- 更深一步的消息驱动自治协作
- 从章节级试点向项目级任务市场调度扩展
- 结果引用、恢复机制与审计能力增强
- 合同页进一步主工作区化
- Coordinator 角色进一步收敛

---

## 七、附录：本轮关键代码落点

### 1. 合同与任务模型

- `novel_agent/workflow/contracts.py`

### 2. 任务池

- `novel_agent/workflow/task_pool.py`

### 3. Agent 能力声明

- `novel_agent/agents/base_agent.py`

### 4. 能力注册表

- `novel_agent/agents/capability_registry.py`

### 5. 消息协议扩展

- `novel_agent/agents/message_bus.py`

### 6. Router/Coordinator 合同接入

- `novel_agent/agents/router_agent.py`
- `novel_agent/workflow/coordinator.py`

### 7. 章节链自治试点与 fallback

- `novel_agent/workflow/coordinator.py`

### 8. 专项测试

- `novel_agent/tests/test_supervised_collab_foundation.py`
- `frontend-tests/copilot-workflow.dom.test.js`
- `frontend-tests/copilot-commands.dom.test.js`

### 9. 合同确认闭环接口与前端视图

- `novel_agent/web/routes/chat.py`
- `novel_agent/web/routes/novel.py`
- `novel_agent/web/models/requests.py`
- `novel_agent/web/static/app-core.js`

### 10. 任务池运行态深化

- `novel_agent/workflow/coordinator.py`
- `novel_agent/web/routes/novel.py`
- `novel_agent/web/static/app-core.js`
- `novel_agent/tests/test_supervised_collab_foundation.py`

### 11. 主工作区任务池页面化与主动轮询

- `novel_agent/web/static/continuous_write.js`
- `novel_agent/web/static/app-core.js`
- `frontend-tests/copilot-workflow.dom.test.js`
- `frontend-tests/copilot-commands.dom.test.js`

### 12. 第五至第十八阶段：章节级任务市场与项目级正式任务池调度深化

- [`NovelCoordinator._build_chapter_autonomous_task_pool()`](../novel_agent/workflow/coordinator.py:1696)
- [`NovelCoordinator._execute_chapter_task_market()`](../novel_agent/workflow/coordinator.py:1825)
- [`NovelCoordinator.execute_project_ready_tasks()`](../novel_agent/workflow/coordinator.py:1403)
- [`NovelCoordinator._load_project_outline_rows()`](../novel_agent/workflow/coordinator.py:1313)
- [`NovelCoordinator._load_project_previous_chapters()`](../novel_agent/workflow/coordinator.py:1320)
- [`NovelCoordinator._persist_project_ready_chapter_result()`](../novel_agent/workflow/coordinator.py:1340)
- [`NovelCoordinator._run_autonomous_task()`](../novel_agent/workflow/coordinator.py:2401)
- [`confirm_creation_contract()`](../novel_agent/web/routes/novel.py:193)
- [`get_status()`](../novel_agent/web/routes/novel.py:240)
- [`novel_agent/tests/test_supervised_collab_foundation.py`](../novel_agent/tests/test_supervised_collab_foundation.py)
- [`showCollabTaskDetail()`](../novel_agent/web/static/continuous_write.js:481)
- [`getCollabNextReadyTasks()`](../novel_agent/web/static/continuous_write.js:461)
- [`getCollabTraceEventTypeLabel()`](../novel_agent/web/static/continuous_write.js:385)
- [`getCollabTraceStageMeta()`](../novel_agent/web/static/continuous_write.js:393)
- [`filterCollabExecutionEvents()`](../novel_agent/web/static/continuous_write.js:443)
- [`renderCollabExecutionTimeline()`](../novel_agent/web/static/continuous_write.js:639)
- [`buildCollabTraceStats()`](../novel_agent/web/static/continuous_write.js:609)
- [`renderCollabTraceFilterBreadcrumbs()`](../novel_agent/web/static/continuous_write.js:639)
- [`getCollabTraceFilters()`](../novel_agent/web/static/continuous_write.js:500)
- [`setCollabTraceFilters()`](../novel_agent/web/static/continuous_write.js:519)
- [`getCollabTraceExpandedEventIds()`](../novel_agent/web/static/continuous_write.js:529)
- [`toggleCollabTraceExpandedEventId()`](../novel_agent/web/static/continuous_write.js:535)
- [`clearCollabTraceExpandedEventIds()`](../novel_agent/web/static/continuous_write.js:550)
- [`renderCollabTaskPoolWorkspace()`](../novel_agent/web/static/continuous_write.js:849)
- [`refreshNovelCollabRuntime()`](../novel_agent/web/static/app-core.js:564)
- [`frontend-tests/copilot-workflow.dom.test.js`](../frontend-tests/copilot-workflow.dom.test.js)
